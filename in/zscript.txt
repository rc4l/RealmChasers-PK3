version "4.3"

// Realm Chasers actor stubs for Ultimate Doom Builder
// Auto-generated from JSON by build_pk3.sh
// Each actor must define editorNumber in range 15000-31000

// From: Start/Start.json
// uniqueId: 15000 → DoomEdNum: 15000
class RC_Start : Actor
{
    Default
    {
        //$Category "Realm Chasers/Actors"
        //$Title "Start"
        //$Help "This is what the player spawns on"
        //$Color 8
        //$NotAngled
        Height 8.0;  // From marker default: 1.0
        Radius 4.0;  // From physicsRadius: 1.0
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Parkour/parkour_start.json
// uniqueId: -460000002 → DoomEdNum: 15011
class RC_Parkour_Start : Actor
{
    Default
    {
        //$Category "Realm Chasers/Parkour"
        //$Title "Parkour Start (Forward)"
        //$Help "Player-interactable entry point for a scripted parkour route, traversing the chain from head to tail. Place at the start of the path and set First Anchor to the tid of the head ParkourAnchor."
        //$Color 14
        //$Arg0 "First Anchor"
        //$Arg0Type 14
        //$Arg0Tooltip "Tid of the head ParkourAnchor in this chain. Hover this thing in UDB to see the arrow to the chain head."
        //$Arg1 "Default Windup Duration (ms)"
        //$Arg1Type 0
        //$Arg1Tooltip "Used for any anchor whose own Windup Duration is 0. 0 = no windup."
        //$Arg2 "Default Travel Duration (ms)"
        //$Arg2Type 0
        //$Arg2Tooltip "Used for any anchor whose own Travel Duration is 0. Must be > 0 — validated at boot."
        //$Arg3 "Default Winddown Duration (ms)"
        //$Arg3Type 0
        //$Arg3Tooltip "Used for any anchor whose own Winddown Duration is 0. 0 = no winddown."
        Height 4.8;  // From interactible.hitboxSize: 0.6
        Radius 1.2;  // From interactible.hitboxSize: 0.3
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Parkour/parkour_anchor_jump.json
// uniqueId: -460000012 → DoomEdNum: 15014
class RC_Parkour_Anchor_Jump : Actor
{
    Default
    {
        //$Category "Realm Chasers/Parkour/Anchors"
        //$Title "Parkour Anchor: Jump Forward"
        //$Help "Path point reached by a single full-body jump from the previous anchor. Use for big gaps (creek crossings, ledge-to-ledge). Consumed at map load — does not spawn as a runtime actor."
        //$Color 14
        //$Arg0 "Next Anchor"
        //$Arg0Type 14
        //$Arg0Tooltip "Tid of the next ParkourAnchor in this chain. 0 = end of route. Hover this thing in UDB to see the link arrow."
        //$Arg1 "Windup Duration (ms)"
        //$Arg1Type 0
        //$Arg1Tooltip "Time spent in the windup animation before traveling to this anchor. 0 = inherit (carry-forward to ParkourStart's Default Windup Duration)."
        //$Arg2 "Travel Duration (ms)"
        //$Arg2Type 0
        //$Arg2Tooltip "Time spent traveling from the previous anchor's position to this anchor. 0 = inherit (carry-forward to ParkourStart's Default Travel Duration)."
        //$Arg3 "Winddown Duration (ms)"
        //$Arg3Type 0
        //$Arg3Tooltip "Time spent in the winddown/landing animation after arriving at this anchor. 0 = inherit (carry-forward to ParkourStart's Default Winddown Duration)."
        Height 8.0;  // From marker default: 1.0
        Radius 0.4;  // From physicsRadius: 0.1
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Parkour/parkour_start_reverse.json
// uniqueId: -460000003 → DoomEdNum: 15015
class RC_Parkour_Start_Reverse : Actor
{
    Default
    {
        //$Category "Realm Chasers/Parkour"
        //$Title "Parkour Start (Reverse)"
        //$Help "Player-interactable entry point for a scripted parkour route, traversing the chain from tail to head. Place at the FAR end of the path and set First Anchor to the tid of the head ParkourAnchor (same value as the forward Start). The runtime walks the baked chain in reverse order."
        //$Color 14
        //$Arg0 "First Anchor"
        //$Arg0Type 14
        //$Arg0Tooltip "Tid of the head ParkourAnchor in this chain (same value as the forward Parkour Start). Hover this thing in UDB to see the arrow to the chain head."
        //$Arg1 "Default Windup Duration (ms)"
        //$Arg1Type 0
        //$Arg1Tooltip "Used for any anchor whose own Windup Duration is 0. 0 = no windup."
        //$Arg2 "Default Travel Duration (ms)"
        //$Arg2Type 0
        //$Arg2Tooltip "Used for any anchor whose own Travel Duration is 0. Must be > 0 — validated at boot."
        //$Arg3 "Default Winddown Duration (ms)"
        //$Arg3Type 0
        //$Arg3Tooltip "Used for any anchor whose own Winddown Duration is 0. 0 = no winddown."
        Height 4.8;  // From interactible.hitboxSize: 0.6
        Radius 1.2;  // From interactible.hitboxSize: 0.3
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Parkour/parkour_anchor_step_right.json
// uniqueId: -460000011 → DoomEdNum: 15013
class RC_Parkour_Anchor_Step_Right : Actor
{
    Default
    {
        //$Category "Realm Chasers/Parkour/Anchors"
        //$Title "Parkour Anchor: Step Right"
        //$Help "Path point where the player lands on their RIGHT foot during alternating-step parkour. Mirror of parkour_anchor_step_left. Consumed at map load — does not spawn as a runtime actor."
        //$Color 14
        //$Arg0 "Next Anchor"
        //$Arg0Type 14
        //$Arg0Tooltip "Tid of the next ParkourAnchor in this chain. 0 = end of route. Hover this thing in UDB to see the link arrow."
        //$Arg1 "Windup Duration (ms)"
        //$Arg1Type 0
        //$Arg1Tooltip "Time spent in the windup animation before traveling to this anchor. 0 = inherit (carry-forward to ParkourStart's Default Windup Duration)."
        //$Arg2 "Travel Duration (ms)"
        //$Arg2Type 0
        //$Arg2Tooltip "Time spent traveling from the previous anchor's position to this anchor. 0 = inherit (carry-forward to ParkourStart's Default Travel Duration)."
        //$Arg3 "Winddown Duration (ms)"
        //$Arg3Type 0
        //$Arg3Tooltip "Time spent in the winddown/landing animation after arriving at this anchor. 0 = inherit (carry-forward to ParkourStart's Default Winddown Duration)."
        Height 8.0;  // From marker default: 1.0
        Radius 0.4;  // From physicsRadius: 0.1
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Parkour/parkour_anchor_step_left.json
// uniqueId: -460000010 → DoomEdNum: 15012
class RC_Parkour_Anchor_Step_Left : Actor
{
    Default
    {
        //$Category "Realm Chasers/Parkour/Anchors"
        //$Title "Parkour Anchor: Step Left"
        //$Help "Path point where the player lands on their LEFT foot during alternating-step parkour. Use in pairs alternating with Step Right anchors for stepping-stones-style routes. Consumed at map load — does not spawn as a runtime actor."
        //$Color 14
        //$Arg0 "Next Anchor"
        //$Arg0Type 14
        //$Arg0Tooltip "Tid of the next ParkourAnchor in this chain. 0 = end of route. Hover this thing in UDB to see the link arrow."
        //$Arg1 "Windup Duration (ms)"
        //$Arg1Type 0
        //$Arg1Tooltip "Time spent in the windup animation before traveling to this anchor. 0 = inherit (carry-forward to ParkourStart's Default Windup Duration)."
        //$Arg2 "Travel Duration (ms)"
        //$Arg2Type 0
        //$Arg2Tooltip "Time spent traveling from the previous anchor's position to this anchor. 0 = inherit (carry-forward to ParkourStart's Default Travel Duration)."
        //$Arg3 "Winddown Duration (ms)"
        //$Arg3Type 0
        //$Arg3Tooltip "Time spent in the winddown/landing animation after arriving at this anchor. 0 = inherit (carry-forward to ParkourStart's Default Winddown Duration)."
        Height 8.0;  // From marker default: 1.0
        Radius 0.4;  // From physicsRadius: 0.1
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Marker/Arrow White Marker.json
// uniqueId: -455453621 → DoomEdNum: 15002
class RC_Markerarrowwhite : Actor
{
    Default
    {
        //$Category "Realm Chasers/Markers"
        //$Title "Marker Arrow (White)"
        //$Help "Helps guide players; visible to everyone"
        //$Color 3
        //$NotAngled
        Height 8.0;  // From marker default: 1.0
        Radius 4.0;  // From physicsRadius: 1.0
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Marker/Marker Networked.json
// uniqueId: -451453622 → DoomEdNum: 15005
class RC_Marker_Networked : Actor
{
    Default
    {
        //$Category "Realm Chasers/Markers"
        //$Title "Marker (Networked)"
        //$Help "Networked marker that isnt always cold. Has dontRender set to true so gameobject exists but cant be seen by the camera."
        //$Color 3
        //$NotAngled
        Height 8.0;  // From marker default: 1.0
        Radius 4.0;  // From physicsRadius: 1.0
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}

// From: Marker/Marker (Serverside Only).json
// uniqueId: -455453622 → DoomEdNum: 15001
class RC_Marker : Actor
{
    Default
    {
        //$Category "Realm Chasers/Markers"
        //$Title "Marker (Serverside Only)"
        //$Help "Helps guide players on the server only — not networked to clients. Use marker_networked if clients need to see it."
        //$Color 3
        //$NotAngled
        Height 8.0;  // From marker default: 1.0
        Radius 4.0;  // From physicsRadius: 1.0
    }
    
    States
    {
    Spawn:
        TNT1 A -1;
        Stop;
    }
}
